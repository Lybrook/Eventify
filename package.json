{
  "name": "eventify",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "web:dev": "npm --prefix eventify-frontend run dev",
    "web:build": "npm --prefix eventify-frontend run build",
    "web:typecheck": "npm --prefix eventify-frontend run typecheck"
  }
}
