"use client";

import { FormEvent, useState } from "react";
import { Bot, CheckCircle2, Mail, MessageCircle, Phone, Send, Sparkles } from "lucide-react";

import { api } from "@/lib/api";

export default function SupportPage() {
  const [form, setForm] = useState({ name: "", email: "", issue: "" });
  const [message, setMessage] = useState("");
  const [error, setError] = useState("");
  const [chatOpen, setChatOpen] = useState(false);
  const [chat, setChat] = useState<{ from: "user" | "bot"; text: string }[]>([]);
  const [input, setInput] = useState("");

  const submit = async (event: FormEvent) => {
    event.preventDefault(); setMessage(""); setError("");
    try { await api.submitSupportTicket(form); setForm({ name: "", email: "", issue: "" }); setMessage("Your support request is in the queue. We will get back to you soon."); } catch (err) { setError(err instanceof Error ? err.message : "Could not submit your request."); }
  };
  const sendMessage = () => { if (!input.trim()) return; const text = input.trim(); setChat((items) => [...items, { from: "user", text }]); setInput(""); window.setTimeout(() => setChat((items) => [...items, { from: "bot", text: "Thanks for reaching out. A member of the Eventify team can help with the details." }]), 500); };

  return <section className="bg-sand pb-24"><div className="section-shell pt-10 sm:pt-14"><div className="rounded-[2.5rem] bg-ink p-7 text-white shadow-soft sm:p-12"><p className="eyebrow mb-4 text-[#f7a395]">We are here</p><h1 className="max-w-2xl font-display text-5xl font-bold tracking-[-0.05em] sm:text-6xl">Questions are part of the plan.</h1><p className="mt-5 max-w-xl text-base leading-7 text-white/60">Whether you are finding a ticket or putting an event on the calendar, we are happy to help you get unstuck.</p><div className="mt-8 grid gap-3 sm:grid-cols-3"><a href="mailto:hello@eventify.co.ke" className="rounded-2xl border border-white/10 bg-white/5 p-4 transition hover:bg-white/10"><Mail size={18} className="text-[#f7a395]" /><p className="mt-6 text-sm font-bold">Email us</p><p className="mt-1 text-xs text-white/50">hello@eventify.co.ke</p></a><a href="tel:+254712345678" className="rounded-2xl border border-white/10 bg-white/5 p-4 transition hover:bg-white/10"><Phone size={18} className="text-[#f7a395]" /><p className="mt-6 text-sm font-bold">Call us</p><p className="mt-1 text-xs text-white/50">+254 712 345 678</p></a><button onClick={() => setChatOpen(true)} className="rounded-2xl border border-white/10 bg-white/5 p-4 text-left transition hover:bg-white/10"><MessageCircle size={18} className="text-[#f7a395]" /><p className="mt-6 text-sm font-bold">Start a chat</p><p className="mt-1 text-xs text-white/50">Quick answers, right here</p></button></div></div><div className="mx-auto mt-10 grid max-w-5xl gap-8 lg:grid-cols-[1fr_0.75fr]"><div className="surface p-6 sm:p-9"><p className="eyebrow mb-3">Support request</p><h2 className="font-display text-3xl font-bold">Tell us what is going on.</h2><p className="mt-3 text-sm leading-6 text-ink/55">A real support ticket goes to the Eventify team, not just your browser console.</p><form onSubmit={submit} className="mt-7 grid gap-4"><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm font-semibold">Name<input className="input-control" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} required /></label><label className="grid gap-2 text-sm font-semibold">Email<input className="input-control" type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} required /></label></div><label className="grid gap-2 text-sm font-semibold">How can we help?<textarea className="input-control min-h-36 resize-y" value={form.issue} onChange={(e) => setForm({ ...form, issue: e.target.value })} required placeholder="Share a little context..." /></label>{message && <div className="flex items-center gap-2 rounded-2xl border border-green-200 bg-green-50 px-4 py-3 text-sm text-green-800"><CheckCircle2 size={17} />{message}</div>}{error && <div className="rounded-2xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<button className="button-primary w-fit"><Send size={16} /> Submit request</button></form></div><div className="rounded-[2rem] border border-ink/10 bg-[#dce9ee] p-7"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-white text-coral"><Sparkles size={22} /></div><h2 className="mt-12 font-display text-3xl font-bold">A few quick answers.</h2><div className="mt-6 grid gap-4 text-sm leading-6 text-ink/65"><div className="rounded-2xl bg-white/70 p-4"><p className="font-bold text-ink">How do payments work?</p><p className="mt-1">M-Pesa prompts are tied to a specific ticket and update automatically after confirmation.</p></div><div className="rounded-2xl bg-white/70 p-4"><p className="font-bold text-ink">Where are my tickets?</p><p className="mt-1">Open My tickets from the navigation after signing in.</p></div><div className="rounded-2xl bg-white/70 p-4"><p className="font-bold text-ink">Can I list an event?</p><p className="mt-1">Create an organizer account and speak with the team for access.</p></div></div></div></div></div>{chatOpen && <div className="fixed bottom-5 right-5 z-50 flex h-[32rem] w-[min(24rem,calc(100vw-2.5rem))] flex-col overflow-hidden rounded-[2rem] bg-white shadow-2xl ring-1 ring-ink/10"><div className="flex items-center justify-between bg-ink p-5 text-white"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-coral"><Bot size={18} /></span><div><p className="font-display font-bold">Eventify support</p><p className="text-xs text-white/50">Usually replies instantly</p></div></div><button onClick={() => setChatOpen(false)} className="text-white/50 hover:text-white">×</button></div><div className="flex-1 space-y-3 overflow-y-auto bg-sand p-4">{chat.length === 0 && <div className="rounded-2xl bg-white p-4 text-sm leading-6 text-ink/65">Hi. What can we help you figure out?</div>}{chat.map((item, index) => <div key={index} className={`max-w-[85%] rounded-2xl p-3 text-sm leading-6 ${item.from === "user" ? "ml-auto bg-ink text-white" : "bg-white text-ink/65"}`}>{item.text}</div>)}</div><div className="flex gap-2 border-t border-ink/10 p-3"><input className="input-control" value={input} onChange={(e) => setInput(e.target.value)} onKeyDown={(e) => e.key === "Enter" && sendMessage()} placeholder="Type a message..." /><button onClick={sendMessage} className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-coral text-white"><Send size={17} /></button></div></div>}</section>;
}
